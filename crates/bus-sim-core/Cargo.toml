[package]
name = "bus-sim-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Native bus fleet-control kernel (Rust port of the Python oracle)"

[lib]
name = "bus_sim_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
